"Use strict"; // treat all JS code as newer version

// alert(3+3) // will pose an error as using alert has a diffrent syntax in node in "browser it will run"

let name= "hitesh"      
let age = 18           
let isLoggedIn = false 

// string
// number => 2 to pwr 53
// boolean => true/ false
// null => standalone value not undefined it is used when we don't want 0 but still want it to be empty
// undefined
// symbol => will use in react (uniqueness)


// object

console.log(typeof"hitesh"); // string
console.log(typeof null);// null is an object(asked in interviews)
console.log(typeof undefined); // o/p undefined